Para instalar los paquetes requeridos se debe ejecutar el siguiente comando dentro la carpeta del proyecto:

npm install

Una vez instalados todos los paquetes se procede a ejecutar el proyecto

npm run dev
